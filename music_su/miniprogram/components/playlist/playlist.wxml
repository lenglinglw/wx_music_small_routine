<view class="playlist-container" bindtap="goMusicList">
  <image src="{{playlist.picUrl}}" class="playlist-img"></image>
  <text class="playlist-playCount">{{playCount}}</text>
  <text class="playlist-name">{{playlist.name}}</text>
</view>